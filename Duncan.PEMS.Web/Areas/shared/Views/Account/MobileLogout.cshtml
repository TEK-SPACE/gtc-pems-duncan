@using Duncan.PEMS.Utilities

@if ( Request.IsAuthenticated )
{
  @Html.ActionLink( HttpContext.Current.GetLocaleResource( ResourceTypes.Glossary, "Logout" ) , "Logoff", "Account", new {@id = "logout", @title = "Logout"} )
}